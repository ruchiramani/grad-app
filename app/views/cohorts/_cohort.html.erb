<% @students = cohort.students %>
<% @entry = Entry.new %>

<%= form_for @entry do |f| %>
<%= f.collection_select :user, @students, :id, :first_name %>
 <%= f.text_field :name, placeholder: 'your name' %>
 <%= f.text_field :content, placeholder: 'message' %>
  <%= f.submit 'Submit' %>

  <% end %>
