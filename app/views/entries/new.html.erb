<% @entry = Entry.new %>
<%= form_for @entry do |f| %>
<% if current_user.cohorts[0] %>
<% @students = current_user.cohorts[0].students %>
<%= f.collection_select :user_id, @students, :id, :first_name %>
 <!-- <%= f.text_field :name, placeholder: 'your name' %> -->
 <%= f.text_field :content, placeholder: 'message' %>
  <%= f.submit 'Submit' %>
  <% end %>

  <% end %>
